[package]
name = "atiny"
version = "0.1.0"
edition = "2021"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
lalrpop-util = {version = "0.18.0", features = ["lexer"]}
im = "*"

[build-dependencies]
lalrpop = {version = "0.18.0", features = ["lexer"]}