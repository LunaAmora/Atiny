type List t = | Cons t (List t) | Nil
type Unit = | Unit
type Bool = | true | false

fn ata (x: List Int) : Int {
    match Nil {
        Cons true (Cons _ true) => 2,
        Nil                     => 4,
    }
}