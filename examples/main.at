use Lib::List
use Lib::Bool

type Something = {
    value: List Bool
}

fn main (other: Lib::OtherThing): (Lib::OtherThing, Something, (List Bool)) {
    let b = other.other;
    (other, b, b.value)
}

fn main2 : (Lib::OtherThing, Something, (List Bool)){
    let other = Lib::OtherThing {
        other = Something {
            value = Cons False Nil
        }
    };

    main other
}