use Main

type List t = | Cons t (List t) | Nil
type Bool   = | True | False

type OtherThing = {
    other: Main::Something
}

fn lib_main : (OtherThing, Main::Something, (List Bool)) {
    let arg = OtherThing {
        other = Main::Something {
            value = Cons False Nil
        }
    };

    Main::main arg
}